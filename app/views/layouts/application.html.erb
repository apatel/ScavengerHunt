<!DOCTYPE html>
<html>
<head>
  <title>ScavengerHunt</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	<div id="utility"><h1><%= link_to "Scavenger Hunt", root_url %></h1></div>
	<div id="utility">
		<% if team_signed_in? %>
          Signed in as <%= current_team.login %>. Not you?
          <%= link_to "Sign out", destroy_team_session_path, :method => :delete %> or <%= link_to "edit account", edit_team_registration_path %>
		<% else %>
		  <%= link_to "Sign in", new_team_session_path %>
        <% end %> 
    </div>

<%= yield %>

</body>
</html>
